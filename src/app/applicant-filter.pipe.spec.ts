import { ApplicantFilterPipe } from './applicant-filter.pipe';

describe('ApplicantFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ApplicantFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
